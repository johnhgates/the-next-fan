# Seen. Scrolled. Sold?

Render/GitHub package for John Gates.

Keep Render settings:
- Root Directory: the-next-fan-render-github
- Publish Directory: dist
- If your Render service is a Web Service, Start Command: npx --yes serve -s dist -l $PORT
- If it is a Static Site, no Start Command is needed.
